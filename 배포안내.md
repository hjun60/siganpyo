# Luna's 시간표 — 배포 안내

웹사이트 주소: **https://stalwart-otter-04c61d.netlify.app**

---

## 방법 1: GitHub 연결 (자동 업데이트) ★ 추천

파일을 수정한 뒤 **Git에 푸시**하면 Netlify가 자동으로 배포합니다.

### 1단계: 처음 한 번만 설정

1. **GitHub 계정**이 없다면 [github.com](https://github.com)에서 가입
2. **GitHub 저장소 만들기**
   - GitHub → New repository
   - 이름: `luna-timetable` (또는 원하는 이름)
   - Public 선택 → Create
3. **Netlify에서 Git 연결**
   - [app.netlify.com](https://app.netlify.com) 로그인
   - 기존 사이트(stalwart-otter-04c61d) 클릭
   - **Site configuration** → **Build & deploy** → **Link repository**
   - GitHub 선택 → 방금 만든 저장소 연결
   - Build settings: **Build command** 비움, **Publish directory** = `.` 또는 ` / `
   - Save

4. **로컬에서 Git 초기화 및 푸시**
   ```bash
   cd "c:\Users\user\Desktop\시간표"
   git init
   git add .
   git commit -m "초기 저장"
   git branch -M main
   git remote add origin https://github.com/내계정/luna-timetable.git
   git push -u origin main
   ```

### 2단계: 수정 후 업데이트 (매번)

```bash
cd "c:\Users\user\Desktop\시간표"
git add .
git commit -m "내용 수정"
git push
```

푸시 후 1~2분 내에 웹사이트가 자동으로 업데이트됩니다.

> **팁**: [GitHub Desktop](https://desktop.github.com/)을 설치하면 명령어 없이 버튼으로 커밋·푸시할 수 있습니다.

---

## 방법 2: Netlify CLI (한 번에 배포)

명령 한 번으로 바로 배포합니다.

### 1단계: 처음 한 번만

1. **Node.js 설치**: [nodejs.org](https://nodejs.org)에서 다운로드
2. **Netlify CLI 설치** (명령 프롬프트 또는 PowerShell):
   ```
   npm install -g netlify-cli
   ```
3. **로그인 및 사이트 연결**:
   ```
   cd "c:\Users\user\Desktop\시간표"
   netlify login
   netlify link
   ```
   - `link` 시 기존 사이트(stalwart-otter-04c61d)를 선택

### 2단계: 수정 후 배포

**deploy.bat** 더블클릭  
또는:

```
cd "c:\Users\user\Desktop\시간표"
netlify deploy --prod
```

---

## 방법 3: 드래그 앤 드롭 (수동)

1. [app.netlify.com](https://app.netlify.com) 로그인 → 해당 사이트 클릭
2. **Deploys** 탭으로 이동
3. 페이지 하단 **"Need to update your site? Drag and drop your site output folder here"** 영역에 **시간표 폴더**를 드래그하여 드롭
4. 같은 URL로 배포 완료

> ⚠️ [app.netlify.com/drop](https://app.netlify.com/drop)은 **새 사이트를 만들 때**만 사용합니다. 기존 사이트를 업데이트하려면 사이트 대시보드의 Deploys 탭에서 드롭하세요.

---

## 요약

| 방법 | 난이도 | 자동 업데이트 |
|------|--------|---------------|
| 1. GitHub 연결 | 중 | ✅ push 시 자동 |
| 2. Netlify CLI | 하 | ⚡ deploy.bat 한 번 |
| 3. 드래그 앤 드롭 | 매우 쉬움 | ❌ 매번 수동 |

**권장**: 방법 1(GitHub)을 설정해 두면, 수정 후 `git push`만 하면 자동으로 사이트가 업데이트됩니다.
